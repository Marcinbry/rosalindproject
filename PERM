#pierwsza część programu pozwala na zliczenie danych permutacji, jest to silnia danej liczby, tworzony jest zakres, niestety funkcja range nie włącza górnego zakresu,
#jest do danej liczby, ale jej nie wlicza, dlatego trzeba zwiększyć górny zakres o jeden, jednocześnie liczby są dodawane do listy, gdzie można ją wykorzystać 
# do wbydowanych bibliotek, tutaj została wykorzystana "import itertools" co pozwala na skorzystanie z permutacji 
import itertools

def permutacja(liczba):
    sum = 1
    lista = []
    for a in range(1, liczba + 1):
        lista.append(a)
        sum *= a
    print(sum)
    from itertools import permutations
    lista1 = "".join(map(str, lista))
    permList = permutations(lista1)
    for perm in list(permList):
            print(' '.join(perm))
print(permutacja(5))
